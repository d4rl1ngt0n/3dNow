export function analyzeGeometry(geometry){geometry.computeBoundingBox();const pos=geometry.getAttribute('position');if(!pos)return null;let area=0,vol=0;for(let i=0;i<pos.count;i+=3){const a=[pos.getX(i),pos.getY(i),pos.getZ(i)],b=[pos.getX(i+1),pos.getY(i+1),pos.getZ(i+1)],c=[pos.getX(i+2),pos.getY(i+2),pos.getZ(i+2)];const ab=b.map((v,j)=>v-a[j]),ac=c.map((v,j)=>v-a[j]),cross=[ab[1]*ac[2]-ab[2]*ac[1],ab[2]*ac[0]-ab[0]*ac[2],ab[0]*ac[1]-ab[1]*ac[0]];area+=Math.hypot(...cross)/2;vol+=(a[0]*cross[0]+a[1]*cross[1]+a[2]*cross[2])/6;}const box=geometry.boundingBox;return {volumeCm3:Math.abs(vol)/1000,surfaceAreaCm2:area/100,triangleCount:pos.count/3,bboxMm:{x:box.max.x-box.min.x,y:box.max.y-box.min.y,z:box.max.z-box.min.z}};}
