{
  "name": "3dnow-student-quote",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite build --watch\" \"node --env-file-if-exists=.env --watch server/index.js\"",
    "build": "vite build",
    "start": "node --env-file-if-exists=.env server/index.js",
    "test": "NODE_ENV=test node --test test/*.test.js",
    "app": "docker compose up --build"
  },
  "dependencies": {
    "express": "^4.21.2",
    "fflate": "^0.8.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^9.0.3",
    "stripe": "^22.3.1",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vite": "^6.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
